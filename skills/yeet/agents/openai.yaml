interface:
  display_name: "Yeet"
  short_description: "Validated task → commit → publish/integrate → prove → retire"
  default_prompt: "Use $yeet to commit, publish or integrate, prove, and retire this validated registered task."
policy:
  allow_implicit_invocation: false
