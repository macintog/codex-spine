interface:
  display_name: "Yeet"
  short_description: "Finish the registered Git transaction"
  default_prompt: "Use $yeet to take this validated registered task to its configured terminal Git disposition."
policy:
  allow_implicit_invocation: false
