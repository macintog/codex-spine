interface:
  display_name: "Project Continuity"
  short_description: "Keep project purpose, state, and handoffs trustworthy"
  default_prompt: "Use $project-continuity to inspect this project's continuity, identify decisions that stale or missing context could distort, and recommend the smallest repair."
policy:
  allow_implicit_invocation: true
