interface:
  display_name: "Project Continuity"
  short_description: "Keep long-lived projects coherent across sessions"
  default_prompt: "Use $project-continuity to inspect, repair, resume, or hand off this project's continuity packet."
policy:
  allow_implicit_invocation: true
