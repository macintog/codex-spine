interface:
  display_name: "Project Continuity"
  short_description: "Keep continuity subject-bound and non-recursive"
  default_prompt: "Use $project-continuity to inspect or repair this project's continuity packet without letting stale or nested artifacts select work."
policy:
  allow_implicit_invocation: true
